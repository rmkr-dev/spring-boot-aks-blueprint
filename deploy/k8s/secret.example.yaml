# EXAMPLE ONLY — copy out of band; do not commit real values.
# Reference pattern for a Kubernetes Secret. Not applied by CI.
# Prefer creating Secrets with kubectl/sealed-secrets/External Secrets rather than git.
apiVersion: v1
kind: Secret
metadata:
  name: spring-boot-aks-blueprint-secrets
  labels:
    app.kubernetes.io/name: spring-boot-aks-blueprint
type: Opaque
stringData:
  # EXAMPLE_API_TOKEN: ""   # set out of band — never commit a real token
  # EXAMPLE_DB_PASSWORD: "" # this blueprint has no database; shown as shape only
